{
  "name": "arknights-training-room",
  "version": "1.0.10",
  "description": "训练室：明日方舟本地专精规划桌面应用",
  "repository": "https://github.com/7aroland/Arknights-Training-Room.git",
  "main": "dist/src/main.js",
  "author": "Arknights Training Room contributors",
  "license": "MIT",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "assets:sync": "node scripts/sync-prts-assets.mjs",
    "build": "npm run clean && tsc && node scripts/copy-static.mjs",
    "start": "npm run build && electron .",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "dist": "npm test && electron-builder --win nsis portable"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/qrcode": "^1.5.5",
    "electron": "^37.2.6",
    "electron-builder": "^26.0.12",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "qrcode": "^1.5.4"
  },
  "build": {
    "appId": "io.github.arknights.trainingroom",
    "productName": "训练室",
    "directories": {
      "output": "release"
    },
    "files": [
      "build/icon.png",
      "dist/**/*",
      "renderer/**/*",
      "resources/images/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "resources/game-data",
        "to": "game-data"
      }
    ],
    "win": {
      "icon": "build/icon.png",
      "target": "nsis"
    },
    "publish": {
      "provider": "github",
      "owner": "7aroland",
      "repo": "Arknights-Training-Room",
      "releaseType": "release"
    },
    "asar": true,
    "nsis": {
      "artifactName": "Arknights-Training-Room-${version}-Setup-${arch}.${ext}",
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "训练室"
    },
    "portable": {
      "artifactName": "Arknights-Training-Room-${version}-Portable-${arch}.${ext}"
    }
  }
}
