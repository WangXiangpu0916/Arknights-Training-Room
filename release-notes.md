## 训练室 v0.0.20-beta.8（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 统计页取消展开/收起，所有星级明细常驻显示；总项与子项统一使用对齐的小型进度条、数值和百分比列。
- 优化统计项中文名称与信息层级，总项加粗，星级子项缩进显示。
- 设置页改为轻量列表布局，移除“连续专精排序”设置，并将“应用更新”移动到倒数第二项。

## 训练室 v0.0.20-beta.7（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 统计页将 M3、E1、E2 等缩写改为“专精三级”“精英阶段1”“精英阶段2”等中文名称。
- 删除统计模块旁的全部口径说明文字，进一步简化页面信息层级。

## 训练室 v0.0.20-beta.6（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 统计页由大型仪表盘卡片改为按“干员持有、技能专精、模组、精英化”组织的轻量列表，统一数字列、细进度线和星级展开明细。
- “全部已实装 / 仅已持有”改为紧凑的全局范围切换控件，保留原有实时统计口径与交互。
- 模组解锁/三级模组及 E1+/E2 分别归并到同一模块，移除重复卡片标题和当前 E0/E1/E2 人数分布。
- 完善统计页深色/浅色主题、布局、范围切换和展开状态的自动化验收。

## 训练室 v0.0.20-beta.5（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 新增“统计”页，可在全量实装与已持有范围间切换，查看持有率、M3、模组解锁/三级及精英化进度，并按星级展开明细。
- 统计口径改为按技能、按模组实例和真实可精英阶段分别计算；精英阶段分布仅统计已持有干员。
- 缓存状态提示改为不挤压页面的悬浮通知，支持手动关闭，并在 5 秒后自动消失。
- 精英化规划卡片改为显示干员当前 E0 / E1 / E2 图标与真实等级。
- 扩展完整干员数据模型、统计回归测试及 Electron / Chromium 视觉验收。

## 训练室 v0.0.20-beta.4（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 优化模组规划卡片视觉，移除模组等级素材外围背景与边框。
- 将模组图标改为保持原始比例的透明正方形展示，并统一模组编号、名称的主题文字色。
- 优化精英化规划卡片布局，使精英化图标视觉底边与干员头像对齐。
- 参考 PRTS「等级图标」新增本地底图与动态等级数字组合，支持任意合法等级。
- 完善模组阶段、模组类型与等级组件的浅色 / 深色主题适配和 Electron 几何回归验证。

## 训练室 v0.0.20-beta.3（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 修复精英化与模组规划的连续模式：仅在连续阶段材料全部满足且跨度至少两级时展示计划。
- 模组规划移除重复提示，使用本地 PRTS 模组类型图标、类型代码、中文名称和 STAGE 1/2/3 官方素材；未装配阶段显示为“未装配”。
- 精英化规划卡片改用精英阶段图标表示目标，详情与连续规划统一使用图标化阶段过渡。
- 补充本地模组素材同步映射、回归测试和浏览器视觉验收。

## 训练室 v0.0.20-beta.2（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 干员页职业水印替换为高清透明素材，消除低分辨率锯齿和背景色块，并适配深色、浅色主题。
- 职业水印移动至 Rank 7 右侧；阿米娅 7 个技能恢复为与普通干员一致的尺寸并改为横向单行，技能组右边缘保持对齐。
- 清理历代本地构建、QA 缓存和已被高清素材替代的旧职业小图，同步脚本不再重新生成废弃素材。

## 训练室 v0.0.20-beta.1（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 精英化与模组规划新增搜索、职业/星级/当前阶段筛选、连续规划和无限池材料开关；连续模式会逐阶段共享并扣减同一份库存。
- 精英化规划补齐升级经验、升级龙门币、晋升龙门币和材料总计，支持 E0→E2；模组规划支持 0→2、0→3、2→3，并显示官方模组类型。
- 三类规划卡片统一头像、六星橙金至红色纵向渐变和信息层级，移除“材料已就绪”占位，释放名称显示空间。
- 干员页新增默认“培养状态”排序；阿米娅 7 技能压缩为固定宽度双行布局；职业文字水印改为职业图标，卡片外观改为列表分隔线。
- 仓库详情改为固定、独立滚动侧栏；“可合成 × N”升级为共享库存的多级递归模拟，并包含循环依赖防护。
- 同步官方模组类型、完整模组材料/龙门币、成长曲线和经验卡数值，以及 8 个职业图标；新增对应计算与 UI 回归测试。

## 训练室 v0.0.19-beta.1（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 新增与专精规划同级的“精英化规划”和“模组规划”，使用森空岛真实养成状态与仓库库存独立计算候选。
- 模组规划支持同一干员多个模组、Lv.0→Lv.3 分段升级，并显示 PRTS 真实模组图片。
- 干员卡片重新整理精英阶段、Rank、专精角标、六档星级、标签及职业水印。
- 仓库详情改为共享页面滚动，展示 PRTS 材料用途/描述及基于真实库存、仅直接配方的“可合成 × N”。
- 新增跟随系统、深色、浅色三态主题；修复更新错误长字符串撑破设置卡片，并将原始错误写入脱敏日志。
- 同步 456 名干员标签、92 项材料说明、492 个模组中文名及精英阶段图标；新增规划与展示回归测试。

## 训练室 v0.0.18-beta.5（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 修复仓库材料选中框与图标不同心；合成配方移除加号并改为三列网格布局。

## 训练室 v1.0.18-beta.4（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 仓库页移除材料图标外圈品质描边；常规材料 4 项置顶单独占第一行；合成配方仅展示所需原料。

## 训练室 v1.0.18-beta.3（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 仓库页改为图标网格浏览：首页仅显示材料图标与持有数量；点击后在右侧详情抽屉查看名称、当前数量、无限供应开关与图标化合成配方。

## 训练室 v1.0.18-beta.2（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 仓库页按常规材料、职业芯片、技巧概要三类分区展示；排除模组资源，各分类独立排序，搜索保持分类结构。

## 训练室 v1.0.18-beta.1（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 仓库页改为双列材料卡片：移除等级/规划状态列与“已设无限”提示；展示全部品质材料，RMA70-12/研磨石/轻锰矿/扭转醇固定置顶，其余按品质倒序。

## 训练室 v1.0.17-beta.9（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 干员页专精角标改为 26×24 px 不透明背景，22×20 px PRTS PNG 内缩 2 px 留白；整体位置 `left/top: -3px`，不改 PNG 圆点。

## 训练室 v1.0.17-beta.8（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 干员页专精角标改为 wrapper + PRTS 透明 PNG 结构：22×21 px 不透明背景 `#1F1A1C`，22×20 px 原图叠放；位置再左/上各 3 px。

## 训练室 v1.0.17-beta.7（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 修复干员页专精角标背景透明问题：PRTS 角标 PNG 圆点以外区域本身透明，现已合成灰色方底（#505050）并保留 CSS 底色兜底。

## 训练室 v1.0.17-beta.6（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 重新打包确认干员页专精角标位置（左/上各偏移 3 px）与 PRTS 灰色背景显示；设置页可见 v1.0.17-beta.6 即为最新构建。

## 训练室 v1.0.17-beta.5（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 干员页专精角标向左、向上各偏移 3 px；保留 PRTS 角标 PNG 自带灰色背景完整显示。

## 训练室 v1.0.17-beta.4（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 修复干员页专精角标被错误拉伸至整个技能图标的问题；角标现为 15×14 px，约占技能图标 23%，贴近游戏本体比例。
- 47 项单元/配置回归全部通过。

## 训练室 v1.0.17-beta.3（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 干员页技能专精等级改为 PRTS 游戏角标 PNG（22×20），替换原有 M0/M1/M2/M3 文字标签；技能图标尺寸与间距不变。
- 47 项单元/配置回归和 Electron 真实交互 QA 全部通过。

## 训练室 v1.0.17-beta.2（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 干员页「筛选」与「更多筛选」改为点击标题展开/收起；鼠标悬停不再改变面板状态，面板内连续调整条件时保持打开。
- 47 项单元/配置回归和 Electron 真实交互 QA 全部通过。

## 训练室 v1.0.17-beta.1（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 收窄专精卡片并优化卡片内部留白；固定窗口调整为 1360×800，仍稳定显示三列且不产生横向溢出。
- 干员搜索改为中文包含匹配、完整拼音和拼音首字母前缀匹配，支持大小写、声调和标点归一化，并保持输入框、光标和中文 IME 稳定。
- 将 11 个筛选维度整理为“筛选”和“更多筛选”两组悬停面板，支持多选、全选、单组清除和全局清除。
- 统一特殊元数据语义：未知、未公开与无法归类项会进入稳定的规范选项，同时保留原始资料。
- 新增实装时间、名称与星级的升降序排序；实装时间来自 PRTS Cargo 的 `cnOnlineTime`，456 名干员均已写入本地缓存。
- 筛选面板采用紧凑横向布局和统一选中态，选项点击后保持面板打开，离开区域后短暂延迟关闭以避免闪烁。
- 未增加运行时依赖；47 项单元/配置回归和 Electron 真实交互 QA 全部通过。
