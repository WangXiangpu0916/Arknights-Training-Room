## 训练室 v1.0.16-beta.1（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 干员页与专精规划统一使用当前规划候选结果，修复真实仓库下的技能蓝框过期问题。
- 新增结构化多选筛选：稀有度、性别、位置、获得方式、职业、分支、种族、出身、组织、团队与生日月份。
- 新增本地完整拼音和首字母索引，支持 `a` / `as` / `asi` / `askl` 等联想搜索，不依赖运行时网络。
- 修复干员搜索每输入一个字符就失焦的问题，搜索时只更新结果区，完整支持中文 IME composition。
- 专精候选在原有专精优先级内增加星级降序次级排序，并保留稳定的名称/技能顺序。
- 窗口固定为 1530×800，锁定三列专精布局，禁止调整大小与最大化，保留最小化、移动和关闭。
- 移除固定窗口后不再使用的 2/3/4 列 breakpoint、ResizeObserver 与 resize-only nested FLIP 路径。
- 新增 PRTS Cargo 本地元数据同步命令与 Electron 真实交互 QA；44 项自动化回归全部通过。
