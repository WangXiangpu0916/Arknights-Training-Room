## 训练室 v1.0.13-beta.1（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 为“当前可以专精”的两列、三列和四列切换增加轻量 FLIP 卡片位置动画。
- 仅在实际列数变化时触发，普通同列宽度调整不会重复计算卡片布局或播放动画。
- 支持快速连续调整窗口宽度时取消旧动画，并在结束后清理临时状态。
- 支持系统“减少动态效果”设置；未增加任何第三方动画依赖。
