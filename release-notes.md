## 训练室 v1.0.15-beta.1（测试版）

> 本版本为测试版，并非正式版；可能存在缺陷或不兼容变更。

- 保留现有两列、三列、四列响应式策略和外层卡片 FLIP 动画，仅在列数实际变化时执行重排。
- 内部 nested FLIP 只测量视口附近的卡片，并收敛为 skill area 单一目标，屏幕外卡片直接完成布局。
- 为候选卡片启用浏览器原生 `content-visibility` 与稳定的固有尺寸占位，显著减少屏幕外内容的 Paint 与 GPU Raster 工作。
- 120-card 压力测试中，最差 P95 frame time 从约 41.7ms 降到约 16.7ms；Paint 总时间和 GPU Raster 峰值均明显下降。
- 动画时长、缓动、卡片尺寸、响应式 breakpoint、筛选与计算逻辑均保持不变，未增加第三方依赖。
